STATUS: DRAFT
SOURCE: pending governance specification

# API

Gabarit réservé à la description d'architecture validée de l'API.

## Extensions prévues

L'API devra pouvoir exposer séparément marchés, lots, bordereaux,
articles, groupes, formules, décomptes, ventilations, indices et
documents. Les imports prévoiront les espaces conceptuels
`imports/price_schedule/` et `imports/statements/` avec mapping
configurable.
